# Skill Registry — jmg-ascensores

_Generated by sdd-init on 2026-04-10_

## Project Info

- **Name**: jmg-ascensores
- **Stack**: Angular 21, TypeScript 5.9, Tailwind CSS 3, SCSS, RxJS 7.8, Vitest
- **Architecture**: Feature-based (Screaming Architecture), Core/Features/Shared layout
- **Testing**: Vitest (via `ng test`)
- **Formatting**: Prettier (printWidth: 100, singleQuote: true, Angular HTML parser)

## Convention Files

No project-level AGENTS.md, CLAUDE.md, or .cursorrules found.

## User Skills

| Skill | Trigger Context |
|-------|----------------|
| `scope-rule-architect-angular` | Angular components, services, templates, architecture decisions |
| `typescript` | TypeScript types, interfaces, generics, strict mode |
| `tailwind-4` | Tailwind CSS styling, cn(), theme variables |
| `playwright` | E2E tests, Page Objects |
| `branch-pr` | Creating pull requests |
| `issue-creation` | Creating GitHub issues |
| `pr-review` | Reviewing PRs and issues |
| `judgment-day` | Adversarial dual review |
| `sdd-explore` | Exploring ideas before committing |
| `sdd-propose` | Change proposals |
| `sdd-spec` | Writing specifications |
| `sdd-design` | Technical design documents |
| `sdd-tasks` | Breaking changes into tasks |
| `sdd-apply` | Implementing tasks |
| `sdd-verify` | Validating implementation |
| `sdd-archive` | Archiving completed changes |
| `skill-creator` | Creating new AI skills |
| `technical-review` | Reviewing technical exercises |
| `stream-deck` | Slide-deck presentations |

## Compact Rules

### scope-rule-architect-angular

**ALWAYS use standalone components** — no NgModules. Use `inject()` not constructor. Use `input()`/`output()` functions. Implement `ChangeDetectionStrategy.OnPush`. Use signals: `signal()`, `computed()`, `effect()`. Use native control flow `@if`/`@for`/`@switch`. No `.component`, `.service`, `.module` suffixes in filenames.

**Scope Rule (UNBREAKABLE)**: Code used by 2+ features → goes in `shared/` or `core/`. Code used by 1 feature → stays inside that feature. NO EXCEPTIONS.

**Screaming Architecture**: Feature names = business domain names. Directory structure tells the story of what the app does. Main feature component name matches the feature name.

**Structure**:
```
src/app/
  features/[feature]/ → local components, services, models, signals
  shared/             → ONLY for 2+ feature usage
  core/               → singleton services, interceptors, guards
```

### typescript

TypeScript strict mode is ON (`strict: true`, `noImplicitReturns`, `noFallthroughCasesInSwitch`, `strictTemplates`). Never use `any`. Use `unknown` for unknown types. Prefer interfaces over types for object shapes. Use generics for reusability.

### tailwind-4

Use Tailwind utility classes directly in templates. Use project's custom color tokens: `primary` (slate-900), `secondary` (slate-700), `accent` (blue-500), `success` (green-500), `warning` (yellow-500), `error` (red-500). SCSS is available for component-specific overrides.
